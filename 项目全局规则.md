# VidLang - 项目全局规则

> **版本**: V1.0 | **日期**: 2026-07-12
> **状态**: 当前有效
>
> ⚠️ **本文件是项目最高优先级规范，所有开发者必须严格遵守。**
> 任何目录结构变更必须先修改本文件，经确认后再执行。

---

## 一、项目概述

本项目为 **VidLang**（视频语言学习应用），采用 **Flutter 单平台 + Supabase 云服务** 架构。
核心功能：通过视频、文章、歌曲三种媒介学习英语，支持字幕查词、跟读评分、测试、单词本等完整学习闭环。

### 1.1 技术栈总览

| 层级 | 技术 |
|------|------|
| 前端框架 | Flutter 3.x (SDK ^3.13.0 beta) |
| 状态管理 | flutter_riverpod (StateNotifierProvider) |
| 本地数据库 | SQLite (sqflite) + FTS5 全文检索 |
| 视频播放 | OmniPlayer（自研，iOS IJK + Android Texture） |
| 音频播放 | just_audio（歌曲/TTS） |
| UI 组件库 | tdesign_flutter（腾讯 TDesign Flutter 版） |
| 屏幕适配 | flutter_screenutil（设计稿 375×812） |
| 后端服务 | Supabase (PostgreSQL + Auth + Storage + Edge Functions) |
| AI 服务 | DeepSeek API（通过 Supabase Edge Functions 包装） |
| 付费方案 | RevenueCat / Stripe（预付费钱包模式） |

### 1.2 架构模式：Flutter Monorepo（单仓多模块）

**为什么选择单仓架构：**
- 多学习引擎（视频/文章/歌曲）共享同一套学习引擎代码
- 本地数据库与云端 Supabase 强耦合
- 团队规模适中，统一管理成本低
- 避免跨包版本协调的复杂性

---

## 二、目录结构规范（⭐ 核心规范）

### 2.1 项目根目录

```
vidlang/                              ← Git 仓库根
│
├── lib/                               ← Flutter 源代码
│   ├── main.dart                      # 应用入口（注册 DB 实体、主题、路由）
│   ├── models/                        # 数据模型（BaseEntity 子类）
│   ├── providers/                     # Riverpod 状态管理
│   ├── services/                      # 服务层（DB、文件、网络、AI）
│   ├── views/                         # 页面（按功能模块划分）
│   ├── components/                    # 公共 UI 组件
│   ├── widgets/                       # 业务组件
│   ├── theme/                         # 主题系统（AppTheme、DesignTokens）
│   └── utils/                         # 工具类
│
├── plugs/                             ← 本地插件（自研/修改的第三方插件）
│   ├── omni_player/                   # 视频播放内核
│   ├── tdesign_flutter/               # TDesign 组件库
│   └── tdesign_flutter_adaptation/    # TDesign 适配层
│
├── supabase/                          ← Supabase 后端
│   ├── functions/                     # Edge Functions（AI 包装、计费等）
│   └── migrations/                    # 数据库迁移脚本
│
├── test/                              ← 测试代码
├── docs/                              ← 项目文档（⭐ 见 2.3 详细规范）
├── assets/                            # 静态资源（Logo、图标、字体等）
├── sherpa-onnx/                       # 语音识别引擎（C++ 本地集成）
│
├── .codebuddy/memory/                 ← AI Agent 记忆（不提交 Git）
├── docs/                              ← 项目文档
├── scripts/                           # 运维脚本
│
├── pubspec.yaml                       # Flutter 依赖配置
├── README.md                          # 项目说明
└── 项目全局规则.md                     ← 本文件（最高规范）
```

### 2.2 源代码目录 (lib/) — 详细规范

#### 2.2.1 数据模型 (lib/models/)

**所有数据模型必须继承 `BaseEntity`，支持软删除和审计字段。**

```
lib/models/
├── base_entity.dart           # 基类（软删除、审计字段、CRUD）
├── video_folder.dart          # 视频集/文件夹
├── video_info.dart            # 视频信息
├── subtitles.dart             # 字幕行（FTS5）
├── participle.dart            # 分词（FTS5）
├── study_record.dart          # 学习记录
├── user.dart                  # 用户
├── config.dart                # 系统配置（KV）
└── ...                        # 未来扩展（Article, Song, WordBook 等）
```

**模型命名规范：**
- 文件名：snake_case.dart（如 `video_folder.dart`）
- 类名：PascalCase（如 `VideoFolder`）
- 表名：snake_case（如 `video_folder`）
- 必须实现 `tableName` getter
- 必须实现 `toMap()` / `fromMap()` 方法

#### 2.2.2 状态管理 (lib/providers/)

**使用 Riverpod 的 StateNotifierProvider 模式：**

```
lib/providers/
├── file_provider.dart         # 文件域状态（文件夹/视频 CRUD、播放状态）
├── navigation_provider.dart   # 导航状态
├── user_provider.dart         # 用户状态
└── ...                        # 未来扩展
```

**Provider 命名规范：**
- 全局单例：`final xxxProvider = Provider<XXX>((ref) => ...)`
- 可变状态：`final xxxNotifierProvider = StateNotifierProvider<XXXNotifier, XXXState>((ref) => ...)`
- 异步数据：`final xxxFutureProvider = FutureProvider<XXX>((ref) => ...)`

#### 2.2.3 服务层 (lib/services/)

**按职责划分，每个服务单一职责：**

```
lib/services/
├── database_service.dart      # 数据库注册、建表、迁移、FTS5
├── file_picker_service.dart   # 文件导入/扫描/复制
├── thumbnail_service.dart     # 视频缩略图生成
├── file_manager_service.dart  # 物理文件 CRUD
├── ai_service.dart            # AI 查询（DeepSeek）
├── tts_service.dart           # 文字转语音
├── auth_service.dart          # 认证（Supabase Auth）
├── sync_service.dart          # 云端同步（Supabase）
└── ...                        # 未来扩展
```

**服务层规则：**
1. 每个服务类必须有完整的文档注释
2. 异步方法必须处理异常（try-catch 或抛出）
3. 数据库操作使用参数化查询（防 SQL 注入）
4. 文件操作需要权限检查

#### 2.2.4 页面 (lib/views/)

**按功能模块划分，每个模块一个目录：**

```
lib/views/
├── main/main_page.dart        # 主页面（底部导航）
├── files/                     # 文件管理模块
│   ├── file_list_page.dart    # 视频集列表
│   └── folder_detail_page.dart # 视频集详情
├── home/home_page.dart        # 首页（Learn Tab）
├── profile/profile_page.dart  # 个人中心
├── player/                    # 播放器模块（未来）
├── reader/                    # 阅读器模块（未来）
└── test/                      # 测试模块
    ├── test_page.dart
    └── test_session_page.dart
```

**页面规则：**
1. 每个 StatefulWidget 必须有对应的 State 类
2. 使用 `const` 构造函数优化性能
3. 页面间导航优先使用命名路由
4. 复杂页面拆分为多个 Part 文件

#### 2.2.5 组件 (lib/components/ & lib/widgets/)

**区分公共组件和业务组件：**

```
lib/components/                # 公共 UI 组件（可复用）
├── folder_card.dart           # 文件夹卡片
├── video_card.dart            # 视频卡片
├── main_video_card.dart       # 主视频卡片
├── avatar.dart                # 头像组件
└── import_progress_dialog.dart # 导入进度弹窗

lib/widgets/                   # 业务组件（特定场景）
├── word_card.dart             # 单词卡片
└── ...                        # 未来扩展
```

**组件规则：**
1. 公共组件放在 `components/`，业务组件放在 `widgets/`
2. 组件必须有完整的文档注释和示例
3. 支持主题适配（使用 `AppTheme` 或 `Theme.of(context)`）
4. 最小点击区域 48×48 dp

#### 2.2.6 主题系统 (lib/theme/)

```
lib/theme/
├── app_theme.dart             # AppTheme 定义（亮色/暗色）
├── app_colors.dart            # 颜色常量
├── app_icons.dart             # 统一图标（Material rounded 线性风格）
├── design_tokens.dart         # 设计令牌（间距、圆角、阴影）
└── text_styles.dart           # 文字样式
```

**主题规则：**
1. 双主题支持：亮色（白色为主）+ 暗色（`#2E302A` 黑绿灰）
2. 所有颜色必须从 `AppColors` 获取，禁止硬编码
3. 图标统一使用 `AppIcons`（Material rounded 线性风格）
4. 间距/圆角等使用 `DesignTokens` 常量

### 2.3 文档目录 (docs/) — 详细规范

| 目录名 | 说明 | 命名规范 |
|--------|------|----------|
| `developer/architecture/` | 架构设计文档（纯架构） | 中文命名 |
| `developer/design/` | 产品/技术设计文档（含 code-knowledge-base） | 中文命名 |
| `developer/design/code-knowledge-base/` | ⭐ 代码知识库/Skill 文件 | 英文/中文 |
| `developer/ai-guides/` | AI 相关文档（Prompt 优化等） | 中文命名 |
| `developer/setup/` | 开发环境配置 | 中文命名 |
| `developer/archive/` | ⚠️ 过期文档备份 | `{原名}-V{版本}-{日期}.md` |
| `designs/` | UI 设计稿（Pencil 格式） | 英文/中文 |
| `detailed-design/` | 详细设计文档（页面级别） | 中文命名 |
| `pages/` | 页面交互说明 | 中文命名 |
| `superpowers/` | AI Agent 能力文档 | 中文命名 |
| `声通/` | 第三方集成文档（声通评分） | 中文 |

**当前 docs 目录需重组**（见第八章操作清单）

---

## 三、文件存放决策树

在项目中新增文件时，按以下流程判断存放位置：

```
新增文件？
│
├─ 是源代码吗？
│  ├─ 是数据模型？       → lib/models/{name}.dart
│  ├─ 是状态管理？       → lib/providers/{name}_provider.dart
│  ├─ 是服务层？         → lib/services/{name}_service.dart
│  ├─ 是页面？           → lib/views/{模块}/{name}_page.dart
│  ├─ 是公共组件？       → lib/components/{name}.dart
│  ├─ 是业务组件？       → lib/widgets/{name}.dart
│  ├─ 是主题相关？       → lib/theme/{name}.dart
│  └─ 是工具类？         → lib/utils/{name}.dart
│
├─ 是本地插件吗？
│  └─ 是自研/修改的插件？ → plugs/{插件名}/
│
├─ 是后端代码吗？
│  ├─ Edge Function？    → supabase/functions/{name}/
│  └─ 数据库迁移？       → supabase/migrations/{timestamp}_{name}.sql
│
├─ 是文档吗？
│  ├─ 架构设计？         → docs/developer/architecture/
│  ├─ 产品/技术设计？    → docs/developer/design/
│  ├─ 代码知识库？       → docs/developer/design/code-knowledge-base/
│  ├─ AI 相关？          → docs/developer/ai-guides/
│  ├─ 开发环境配置？     → docs/developer/setup/
│  ├─ 已过期？           → docs/developer/archive/
│  └─ 其他？             → 按类型归入对应子目录
│
├─ 是静态资源吗？
│  ├─ Logo/图标？        → assets/Logo/ 或 assets/app/
│  ├─ 模型文件？         → assets/models/
│  └─ 视频素材？         → assets/video_plays/
│
└─ 是临时/废弃文件？     → 删除或归档，禁止散落在根目录
```

**绝对禁止的行为：**
- ❌ 在项目根目录放置源代码文件（除 `main.dart` 和配置文件）
- ❌ 在 `lib/` 根目录散落非标准目录的文件
- ❌ 将 `node_modules/`、`.dart_tool/`、`build/` 提交到 Git
- ❌ 在 `plugs/` 中提交 `build/`、`.pub-cache/` 等构建产物
- ❌ 文档中使用过时的技术栈描述（如已更换的依赖包名）

---

## 四、命名规范

### 4.1 Dart 代码命名

| 类型 | 规范 | 示例 |
|------|------|------|
| **类/枚举** | PascalCase | `VideoFolder`, `VideoFolderType` |
| **文件名** | snake_case.dart | `video_folder.dart` |
| **方法/变量** | camelCase | `videoList`, `loadVideos()` |
| **常量** | k + PascalCase | `kDefaultDuration` |
| **私有成员** | _ + camelCase | `_internalState` |
| **数据库表名** | snake_case | `video_info`, `study_record` |
| **数据库字段** | snake_case | `folder_code`, `created_at` |

### 4.2 目录命名

| 场景 | 规范 | 示例 |
|------|------|------|
| **源码目录** | snake_case | `lib/views/files/`, `lib/services/` |
| **插件目录** | kebab-case 或 snake_case | `omni_player/`, `tdesign_flutter/` |
| **文档目录** | 中文 | `架构设计/`, `代码知识库/` |
| **资源目录** | 英文/kebab-case | `video_plays/`, `Logo/` |

### 4.3 文档文件命名

| 类型 | 规范 | 示例 |
|------|------|------|
| **设计文档** | 中文名称-V{版本}.md | `数据库设计-V1.md` |
| **知识库文档** | 中文名称.md 或英文名称.md | `后端代码结构.md`, `API接口文档.md` |
| **归档文档** | `{原名}-V{版本}-{日期}.md` | `数据库设计-V1-20260710.md` |
| **README** | README.md（固定） | `README.md` |

### 4.4 Git 提交信息

```
<type>(<范围>): <描述>

类型：
- feat: 新功能
- fix: 修复 bug
- docs: 文档更新
- style: 代码格式（不影响功能）
- refactor: 重构
- test: 测试相关
- chore: 构建/工具/结构变更

范围（示例）：
- models: 模型改动
- services: 服务层改动
- views: 页面改动
- theme: 主题改动
- docs: 文档改动
- structure: 目录结构变更
- global: 全局配置变更

示例：
feat(services): 添加 AI 查询服务（DeepSeek 集成）
fix(views): 修复文件夹详情页封面显示问题
docs: 更新 AGENT_CONTEXT.md 至 V2
chore(structure): 重组 docs 目录结构
```

---

## 五、开发环境与启动

### 5.1 前置要求

| 环境 | 版本要求 | 说明 |
|------|----------|------|
| **Flutter SDK** | ^3.13.0 beta | 稳定版也可，但部分特性可能受限 |
| **Dart** | 3.x | 与 Flutter SDK 版本匹配 |
| **开发工具** | VS Code / Android Studio / Cursor | 推荐 Cursor（AI 辅助） |
| **平台** | macOS / Windows / Linux | 跨平台支持 |
| **Xcode** | 15+ | iOS 开发必需（macOS） |
| **Android SDK** | 34+ | Android 开发必需 |
| **Node.js** | 18+ | Supabase 本地开发（可选） |

### 5.2 启动顺序

```bash
# 1. 安装 Flutter 依赖
flutter pub get

# 2. 启动应用（默认 iOS 模拟器）
flutter run

# 3. 运行测试
flutter test

# 4. 构建 iOS 发布版
flutter build ios --release

# 5. 构建 Android 发布版
flutter build appbundle --release
```

### 5.3 关键配置文件

| 文件 | 说明 |
|------|------|
| `pubspec.yaml` | Flutter 依赖管理 |
| `analysis_options.yaml` | Dart 代码分析规则 |
| `ios/Podfile` | iOS 原生依赖 |
| `android/app/build.gradle.kts` | Android 构建配置 |
| `supabase/deno.json` | Supabase Edge Functions 配置 |

---

## 六、配置管理规范

### 6.1 敏感信息原则

| 原则 | 说明 |
|------|------|
| **禁止提交** | 密钥、Token、API Key 等敏感信息禁止提交到 Git |
| **本地存储** | 敏感信息存储在环境变量或 `.env` 文件（不提交） |
| **模板提交** | 提供 `.env.example` 作为配置模板 |
| **Supabase 配置** | 使用 `supabase/config.toml` 管理连接信息（不提交密钥） |

### 6.2 环境变量

| 变量名 | 说明 | 示例 |
|--------|------|------|
| `SUPABASE_URL` | Supabase 项目 URL | `https://xxx.supabase.co` |
| `SUPABASE_ANON_KEY` | Supabase 匿名密钥 | `eyJhbGciOi...` |
| `DEEPSEEK_API_KEY` | DeepSeek API 密钥 | `sk-xxx` |
| `REVENUECAT_API_KEY` | RevenueCat 密钥 | `xxx...` |

---

## 七、代码规范（核心要点）

### 7.1 注释要求（强制）

所有代码必须包含清晰的注释，详见 [`docs/DEVELOPMENT.md`](./docs/DEVELOPMENT.md)。

**快速参考：**

```dart
/// 类名描述
///
/// 类的详细功能说明，包括主要用途、功能列表等。
///
/// 示例：
/// ```dart
/// final example = MyClass();
/// ```
class MyClass {
  /// 属性描述
  /// 
  /// 属性的详细说明，包括用途、取值范围、默认值等
  String propertyName;

  /// 方法描述
  /// 
  /// [param1]: 参数1的描述
  /// 
  /// 返回值：返回值的描述
  void myMethod(String param1) {
    // ...
  }
}
```

### 7.2 数据模型规范

**所有模型必须继承 BaseEntity：**

```dart
class VideoInfo extends BaseEntity {
  String name;
  String folderCode;
  
  VideoInfo({
    this.name = '',
    this.folderCode = '',
  });
  
  @override
  String get tableName => 'video_info';
  
  @override
  Map<String, dynamic> toMap() {
    return {
      'name': name,
      'folder_code': folderCode,
      // ... 其他字段
    };
  }
  
  @override
  BaseEntity fromMap(Map<String, dynamic> map) {
    return VideoInfo(
      name: map['name'] ?? '',
      folderCode: map['folder_code'] ?? '',
    );
  }
}
```

### 7.3 时间字段规范

- **数据库存储**：ISO8601 格式字符串
- **时长字段**：
  - 视频相关（duration/position）：**毫秒**
  - 学习记录（StudyRecord.duration）：**秒**
- **显示转换**：使用 `DurationHelper` 工具类

### 7.4 状态管理规范

**使用 Riverpod 的标准模式：**

```dart
// 1. 定义 State
class FileState {
  final List<VideoFolder> folders;
  final bool isLoading;
  final String? error;
  
  FileState({
    this.folders = const [],
    this.isLoading = false,
    this.error,
  });
}

// 2. 定义 Notifier
class FileNotifier extends StateNotifier<FileState> {
  final FileService _fileService;
  
  FileNotifier(this._fileService) : super(FileState());
  
  Future<void> loadFolders() async {
    state = state.copyWith(isLoading: true);
    try {
      final folders = await _fileService.getAllFolders();
      state = state.copyWith(folders: folders, isLoading: false);
    } catch (e) {
      state = state.copyWith(isLoading: false, error: e.toString());
    }
  }
}

// 3. 定义 Provider
final fileProvider = StateNotifierProvider<FileNotifier, FileState>((ref) {
  return FileNotifier(ref.watch(fileServiceProvider));
});
```

---

## 八、文档管理规范

### 8.1 版本管理（严格执行）

| 规则 | 说明 |
|------|------|
| **文件名格式** | `文档名-V{版本号}.md`（设计文档） |
| **版本号递增** | V1 → V2 → V3... |
| **文档头信息** | 必须包含版本号、日期、状态 |
| **归档旧版** | 修改前备份到 `docs/developer/archive/` |

### 8.2 更新流程

1. **修改前备份** → 复制旧版到 `docs/developer/archive/`
2. **更新版本号** → 版本号 +1
3. **同步提交** → 文档与代码在同一 Commit
4. **定期审查** → 每月检查文档与代码一致性

### 8.3 文档与代码一致性

**核心原则：文档修改先于或同步于代码修改**

**特殊说明：**
- `docs/AGENT_CONTEXT.md` 是 AI 上下文速查，**优先级最高**，必须实时更新
- `docs/DEVELOPMENT.md` 是开发规范，相对稳定，重大变更时更新
- 技术栈变更后，必须在所有文档中同步更新（避免过时信息）

---

## 九、新增模块时的操作清单

当需要新增一个功能模块（如文章阅读、歌曲播放）时：

- [ ] 1. 在 `lib/models/` 下创建新模型（继承 BaseEntity）
- [ ] 2. 在 `lib/main.dart` 注册新实体到 DatabaseService
- [ ] 3. 在 `lib/services/` 下创建对应的服务类
- [ ] 4. 在 `lib/providers/` 下创建状态管理
- [ ] 5. 在 `lib/views/` 下创建页面目录和页面文件
- [ ] 6. 如有新组件，在 `lib/components/` 或 `lib/widgets/` 下创建
- [ ] 7. 更新本文「二、目录结构规范」中的目录树
- [ ] 8. 更新 `docs/AGENT_CONTEXT.md` 中的代码结构和实体列表
- [ ] 9. 更新 `docs/developer/design/code-knowledge-base/` 下的知识库文档
- [ ] 10. 更新 `.gitignore`（如有新的忽略需求）
- [ ] 11. 提交 Commit，描述使用 `chore(structure): 新增 {模块名} 模块`

---

## 十、注意事项

1. **单仓纪律**：所有代码必须在 `lib/` 下的规范目录中，严禁散落
2. **双主题支持**：所有 UI 必须适配亮色和暗色主题
3. **图标统一**：使用 `AppIcons`（Material rounded 线性风格），禁止混用不同风格图标
4. **离线优先**：本地 SQLite 为主，云端 Supabase 同步为辅
5. **时长单位注意**：视频用毫秒，学习记录用秒，**严禁混淆**
6. **软删除**：所有实体支持软删除（`is_deleted` 字段），物理删除需谨慎
7. **AI 上下文**：`AGENT_CONTEXT.md` 是给 AI/Cursor 的速查文档，**必须保持准确**
8. **插件管理**：`plugs/` 下的插件是自研或修改过的第三方插件，**不要随意更新**
9. **Supabase Edge Functions**：使用 Deno 运行时，部署前需本地测试
10. **国际化准备**：产品默认英文 UI，但当前阶段中文注释/文档可接受

---

## 十一、相关文档索引

| 文件 | 路径 | 说明 |
|------|------|------|
| **项目全局规则** | `项目全局规则.md` | ⭐ 本文件（最高规范） |
| **AI 上下文速查** | `docs/AGENT_CONTEXT.md` | AI/Cursor 快速恢复认知 |
| **开发规范** | `docs/DEVELOPMENT.md` | 注释/命名/Git 详细规范 |
| **整体架构** | `docs/overall-architecture.md` | 三引擎架构、产品定位 |
| **设计风格指南** | `docs/design-style-guide.md` | UI/UX 规范 |
| **代码知识库** | `docs/developer/design/code-knowledge-base/` | ⭐ 结构化知识库 |
| **README** | `README.md` | 项目简介（面向外部开发者） |

---

**文档版本**：V1.0
**创建时间**：2026-07-12
**最后更新**：2026-07-12（V1.0 初始版本：基于 housing-monitor-platform 经验，建立 VidLang 全局规则）
**更新内容**：
- V1.0: 初始版本，建立完整的目录结构规范、命名规范、文档管理规范
- 参考 housing-monitor-platform 的 `项目全局规则.md` 结构，针对 Flutter 项目定制
